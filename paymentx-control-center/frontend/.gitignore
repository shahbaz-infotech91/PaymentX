# Reproducible build/dependency output - never committed, matches the
# same discipline as every PaymentX Maven module's target/.
node_modules/
dist/
dist-ssr/
*.local

# Real local secrets/config - .env.example is the committed template.
.env
.env.local
.env.*.local

# Editor/OS noise
.vscode/*
!.vscode/extensions.json
.DS_Store
